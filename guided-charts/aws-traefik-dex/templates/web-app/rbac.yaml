{{- if .Values.webApp.enabled }}
# ServiceAccount for the web-app pod
# Note: This service account has NO permissions - all operations use the user's OAuth token
apiVersion: v1
kind: ServiceAccount
metadata:
  name: web-app
  namespace: {{ .Values.namespace }}
  labels:
    app: web-app
    component: web-app
{{- end }}