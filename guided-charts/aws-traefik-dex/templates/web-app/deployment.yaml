{{- if .Values.webApp.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: {{ .Values.namespace }}
  labels:
    app: web-app
    component: web-app
spec:
  replicas: {{ .Values.webApp.replicas }}
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
        component: web-app
    spec:
      serviceAccountName: web-app
      containers:
      - name: web-app
        image: "{{ .Values.webApp.image.repository }}:{{ .Values.webApp.image.tag }}"
        imagePullPolicy: {{ .Values.webApp.image.pullPolicy }}
        ports:
        - containerPort: 8090
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "8090"
        - name: NAMESPACE
          value: {{ .Values.webApp.namespace | quote }}
        - name: STATIC_DIR
          value: "./dist"
        {{- if and .Values.webApp.session .Values.webApp.session.enabled }}
        - name: SESSION_ENABLED
          value: "true"
        - name: SESSION_COOKIE_NAME
          value: {{ .Values.webApp.session.cookieName | quote }}
        - name: SESSION_COOKIE_PATH
          value: {{ .Values.webApp.session.cookiePath | quote }}
        - name: SESSION_COOKIE_MAX_AGE_SECS
          value: {{ .Values.webApp.session.cookieMaxAgeSecs | quote }}
        - name: SESSION_MAX_LIFETIME_SECS
          value: {{ .Values.webApp.session.maxSessionLifetimeSecs | quote }}
        - name: SESSION_NEAR_EXPIRY_THRESHOLD_SECS
          value: {{ .Values.webApp.session.nearExpiryThresholdSecs | quote }}
        - name: SESSION_SECRET_NAME
          value: {{ .Values.webApp.session.secretName | quote }}
        - name: SESSION_SECRET_NAMESPACE
          value: {{ .Values.namespace | quote }}
        - name: SESSION_KEY_PREFIX
          value: {{ .Values.webApp.session.keyPrefix | quote }}
        - name: SESSION_NEW_KEY_USE_DELAY_SECS
          value: {{ .Values.webApp.session.newKeyUseDelaySecs | quote }}
        - name: SESSION_EXPECTED_DOMAIN
          value: {{ .Values.domain | quote }}
        {{- end }}
        resources:
          {{- toYaml .Values.webApp.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
      securityContext:
        fsGroup: 1000
{{- end }}